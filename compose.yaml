services:
  backend:
    image: alvaropereda/backend-rpi
    build: ./backend
    ports:
      - "8081:8080"
    volumes:
      - models:/root/.cache/llama.cpp

  frontend:
    image: alvaropereda/frontend-rpi
    build: ./frontend
    ports:
      - "8082:80"

volumes:
  models: